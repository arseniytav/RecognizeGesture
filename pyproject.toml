[project]
name = "recognize_gesture"
version = "0.1.0"
dependencies = [
    "flet==0.25.2",
    "fastapi",
    "uvicorn",
    "numpy",
    "python-multipart"
]
requires-python = ">=3.10,<4.0"

[tool.flet.app]
product_name = "Sign Language Recognition"
